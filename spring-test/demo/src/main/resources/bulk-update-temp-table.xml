<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="demo.bulkUpsert">
    <sql id="_createTempTable">
        CREATE TEMPORARY TABLE user_temp LIKE `user`
    </sql>

    <sql id="_insertTempTable">
        INSERT INTO user_temp (name, type) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.name}, #{item.type})
        </foreach>
    </sql>

    <sql id="_updateMainTable">
        UPDATE user u
        INNER JOIN user_temp t ON u.name = t.name
        SET u.type = t.type
    </sql>

    <sql id="_dropTempTable">
        DROP TEMPORARY TABLE user_temp
    </sql>

    <update id="createTempTable">
        <include refid="_createTempTable"/>;
    </update>

    <update id="insertTempTable">
        <include refid="_insertTempTable"/>;
    </update>

    <update id="updateMainTable">
        <include refid="_updateMainTable"/>;
    </update>

    <update id="dropTempTable">
        <include refid="_dropTempTable"/>;
    </update>

    <update id="bulkUpdateFromTempTable" parameterType="com.gngsn.demo.bulkUpsert.UserVO">
        <include refid="_createTempTable"/>;

        <include refid="_insertTempTable"/>;
        <include refid="_updateMainTable"/>;

        <include refid="_dropTempTable"/>;
    </update>

    <update id="bulkUpdateMultiLine" parameterType="com.gngsn.demo.bulkUpsert.UserVO">
        <foreach collection="list" item="item" separator=";">
            UPDATE user
            SET type = #{item.type}
            WHERE name = #{item.name}
        </foreach>
    </update>

    <select id="selectUpdateUserCnt" resultType="Integer">
        SELECT count(*)
        FROM user
        WHERE type = 'Update';
    </select>
</mapper>

