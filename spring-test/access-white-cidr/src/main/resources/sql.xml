<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test.whiteCidrDAO">

    <select id="selectWhiteCidrList" resultType="String">
        SELECT cidr
        FROM white_cidr
        WHERE active = 1;
    </select>
</mapper>

